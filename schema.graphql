type _Schema_
  @fulltext(
    name: "entitySearch"
    language: en
    algorithm: rank
    include: [{ entity: "Event", fields: [{name: "type"}] }]
  )
 

type Event @entity {
  id: ID!
  address: String!
  type: String!
  timestamp: BigInt!
  data: [Field!] @derivedFrom(field: "event")
}

type Field @entity {
  id: ID!
  name: String!
  type: String!
  value: String!
  event: Event!
}